spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/poc_auditoria
    username: poc_auditoria
    password: poc_auditoria
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

  flyway:
    enabled: false